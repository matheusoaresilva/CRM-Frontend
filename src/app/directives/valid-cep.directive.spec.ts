import { ValidCepDirective } from './valid-cep.directive';

describe('ValidCepDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidCepDirective();
    expect(directive).toBeTruthy();
  });
});
